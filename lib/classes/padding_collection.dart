import 'package:flutter/material.dart';

class PaddingCollection {
  EdgeInsets signInPadding =
      const EdgeInsets.only(top: 262, left: 27, right: 22, bottom: 127);
  EdgeInsets textFieldPadding =
      const EdgeInsets.only(top: 20, left: 20, right: 24, bottom: 0);

  EdgeInsets iconPadding = const EdgeInsets.all(10);
}
